<summary> ✏ CHAPTER 05 깃을 더 깊게 이해하기 </summary>
<div markdown="1">
    
## 깃의 특징과 기능의 기술적 이해

**깃이 특별한 이유**
1. 스냅샷을 사용한다.
    - 새로운 버전이 만들어질 때 해당 버전의 각 파일이 최종 상태 그대로 저장되는 방식

2. 깃은 분산 버전 관리 시스템이다.
    - 각자 편한대로 작업하다가 원하는 때에 프로젝트를 푸시와 풀로 동기화하면서 협업할 수 있다.


**git rm 명령으로 파일 삭제하기**

git rm (파일 이름) - 삭제하면서 바로 커밋하기


git reset --hard - 앞의 작업 되돌기

**git mv 명령으로 파일 이동하기**

git mv (원래 파일 이름) (변경 파일 이름)

**restor 명령으로 파일을 작업 디렉터리로 되돌리기**

- 위의 결과는 tigers.yaml, pumas.yaml, panthers.yaml 파일이 변경되었다.
- 이 주에서 pumas.yaml은 커밋에 포함하고 싶지 않다면?

git restore --staged (파일 이름)

**변경 사항을 작업 디렉터리에서 빼기**
git restore 파일 이름

**git reset 명령으로 작업 되돌리기**

- git reset --hard: 작업 내역 자체를 지운다.
- git reset --mixed: 변경 사항을 스테이지 영역에서만 제거하는 명령
- git reset --soft: 변경 사항을 저장소에서만 제거하고 스테이지 영역을 남겨 둔다.

## 체크아웃과 페치

**헤드 개념 이해하기**
- `헤드`는 현재 작업 중인 브랜치의 가장 최신 커밋을 나타내는 포인터이다.
- `체크아웃` 명령을 이용하면 헤드를 이동해서 이전 커밋으로 돌아가는 등의 작업을 수행할 수 있다.

헤드는 다음과 같은 상황에서 바뀔 수 있다.
1. 새로운 커밋이 작업 중인 브랜치에 추가될 때마다 헤드가 해당 커밋을 가리킨다.
2. 새로운 브랜치를 생성할때 헤드가 새로운 브랜치를 가리킨다.
3. 다른 브랜치로 이동할때 헤드가 이동한 브랜치를 가리킨다.

`git checkout HEAD^` : 체크아웃으로 한단계만큼 돌아가는 명령어

<img width="532" alt="image" src="https://github.com/nyeongha/systudy1/assets/87464750/c017cdc5-4f07-4c34-8ebf-c583438d0780">

<img width="594" alt="image" src="https://github.com/nyeongha/systudy1/assets/87464750/21b3ebb4-126a-4d5f-a500-8c33f62ce45c">

- 위의 이미지와 같이 브랜치가 main혹은 delta-branch 가 아닌 이전에 없던 임의의 브랜치로 지정되어 있다.
- 즉, checkout 으로 과거로 돌아간다는 것은 해당 시점에 아직 이름이 지어지지 않은 임시 브랜치를 생성한 뒤 그쪽으로 이동하는 것이다.
- 
-----

beta-branch 로 이동하고 전 단계로 헤드를 이동해 보는 실습

<img width="578" alt="image" src="https://github.com/nyeongha/systudy1/assets/87464750/0f12999c-01d3-483f-9467-27a1541ecd75">

헤드를 사용하여 리셋하기
`git reset (옵션) HEAD~(원하는 단계)`

<img width="582" alt="image" src="https://github.com/nyeongha/systudy1/assets/87464750/835f2ea1-266b-438c-bdc1-e0ae0cd10636">

**페치와 풀 차이**
- `풀`: 원격 저장소의 최신 커밋을 로컬 컴퓨터로 가져와 머지하거나 리베이스한다.
- `페치`: 원격 저장소의 최신 커밋을 로컬로 가져오기만 한다.

**원격 저장소의 새 브랜치 확인하기**

<img width="568" alt="image" src="https://github.com/nyeongha/systudy1/assets/87464750/1a1c3e8f-fdf2-41cc-91f0-804f2932e3ea">

## 도움말과 설정 활용하기

**git help 명령으로 도움말 활용하기**
- 깃 명령어와 그에 대한 간단한 설명을 살펴볼 수 있다.
```
git help
git help -a -> 깃의 모든 명령어를 확인
```

`git commit -h` : git commit 명령에 붙는 옵셥을 확인하는 명령어

명령과 옵션을 웹사이트에서 자세히 보고 싶다면 git help 다음에 명령어를 입력하거나 git 다음에 특정 명령어를 입력하고 --help 옵션을 붙인다.
```
git help (명령어)
git (명령어) --help
```

**깃의 각종 설정**
1. global 설정: 해당 컴퓨터의 깃에서 사용하는 모든 저장소에 대한 설정을 변경
2. local 설정: 특정 깃 저장소에 대해서만 설정을 변경

`git config --global user.name (username)` : 사용자 이름 지정

`git config --global user.email (email)` : 사용자 이메일 지정


</details>
